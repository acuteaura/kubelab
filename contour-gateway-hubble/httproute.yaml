apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: hubble-ui
  namespace: kube-system
spec:
  hostnames:
    - hubble.local.gd
  parentRefs:
  - name: default
    namespace: default
  rules:
  - backendRefs:
    - name: hubble-ui
      port: 80